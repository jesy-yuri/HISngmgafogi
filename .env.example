# Database configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=hms_demo

# Server
PORT=3001
NODE_ENV=development

# CORS (set to your frontend origin; for Railway set to your Railway app URL)
CORS_ORIGIN=http://localhost

# Optional: use SQLite locally instead of MySQL
# USE_SQLITE=true
# SQLITE_DB=hms_demo.sqlite
